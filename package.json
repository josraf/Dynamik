{
  "name": "dynamik-ex",
  "version": "1.0.0",
  "description": "This is dynamik exercise",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Jose Domingues",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0"
  },
  "engines": {
    "node": "18.17.0"
  }
}
